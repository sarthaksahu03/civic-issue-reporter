# Copy this file to `.env` in the same directory and fill in your values.
# After creating/updating `.env`, restart the Vite dev server.

# Supabase project URL (e.g., https://xyzcompany.supabase.co)
VITE_SUPABASE_URL=

# Supabase anonymous public API key (from Project Settings -> API -> anon key)
VITE_SUPABASE_ANON_KEY=

# Backend API base URL for the Express server
# Defaults to http://localhost:4000/api if not set
VITE_API_URL=http://localhost:4000/api
